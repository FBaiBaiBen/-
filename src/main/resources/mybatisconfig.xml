<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>

    <!--读取properties文件-->
    <properties resource="jdbc.properties"></properties>

    <settings>
        <!--默认是关闭的，开启驼峰命名自动映射，将数据库中的列名映射到属性名-->
        <setting name="mapUnderscoreToCamelCase" value="true"/>
    </settings>

    <!--指定位置，为该包下的类自动取别名-->
    <typeAliases>
        <package name="com.entity"/>
    </typeAliases>

    <!--分页拦截-->
    <plugins>
        <plugin interceptor="com.github.pagehelper.PageInterceptor"></plugin>
    </plugins>

    <!--配置环境-->
    <environments default="mysql">
        <environment id="mysql">
            <transactionManager type="JDBC"></transactionManager>
            <dataSource type="POOLED">
                <property name="driver" value="${jdbc.driver}"/>
                <property name="url" value="${jdbc.url}"/>
                <property name="username" value="${jdbc.username}"/>
                <property name="password" value="${jdbc.password}"/>
            </dataSource>
        </environment>
    </environments>

    <!--引入mapper映射文件-->
    <mappers>
        <mapper resource="mapper/AccountMapper.xml"></mapper>
        <mapper resource="mapper/DownloadMapper.xml"></mapper>
        <mapper resource="mapper/JurisdictionMapper.xml"></mapper>
        <mapper resource="mapper/MenuMapper.xml"></mapper>
        <mapper resource="mapper/PostMapper.xml"></mapper>
        <mapper resource="mapper/SectionMapper.xml"></mapper>
        <mapper resource="mapper/StaffMapper.xml"></mapper>
    </mappers>

</configuration>